// ======================================
// Health Pilot
// Main Entry Point
// ======================================

console.log("🌿 Health Pilot starting...");

// Application version
const APP = {
    name: "Health Pilot",
    version: "0.4.0",
    aiProvider: "built-in"
};

window.APP = APP;

console.log(`${APP.name} v${APP.version}`);